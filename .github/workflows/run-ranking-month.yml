name: Run Ranking MONTH

on:
  schedule:
    - cron: '15 15 * * *'  # JST 00:15
  workflow_dispatch:

jobs:
  ranking:
    runs-on: ubuntu-latest
    steps:
      - name: Run ranking-updater-month with cursor paging
        run: |
          lastId=""
          LIMIT=200

          while true; do
            echo "MONTH lastId=$lastId"

            RESPONSE=$(curl -s \
              -X POST https://ukdywjvpcsltnrktbbmg.functions.supabase.co/ranking-updater-month \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
              -d "{\"lastId\": \"$lastId\", \"limit\": $LIMIT}")

            echo "Response: $RESPONSE"

            nextLastId=$(echo "$RESPONSE" | jq -r '.nextLastId')

            if [ "$nextLastId" = "null" ] || [ -z "$nextLastId" ]; then
              echo "DONE"
              break
            fi

            lastId=$nextLastId

            sleep 1
          done
