name: Run Ranking YEAR

on:
  schedule:
    - cron: '45 17 * * *'  # JST 02:45
  workflow_dispatch:

jobs:
  ranking:
    runs-on: ubuntu-latest
    steps:
      - name: Run ranking-updater-year in batches
        run: |
          MAX=20000
          BATCH=200

          for ((i=0; i<$MAX; i+=$BATCH)); do
            echo "YEAR offset=$i"

            curl -s -o /dev/null -w "%{http_code}\n" \
              -X POST https://ukdywjvpcsltnrktbbmg.functions.supabase.co/ranking-updater-year \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
              -d "{\"offset\": $i, \"limit\": $BATCH}"

            sleep 1
          done
